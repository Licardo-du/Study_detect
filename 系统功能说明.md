# 学习行为监测系统功能说明

本文档用于说明当前 `study-behavior-monitor-plus` 版本的系统结构、主要功能、功能之间的联系，以及核心实现方式。它可以作为本地调试、课程答辩和项目交付说明使用。

## 1. 系统整体定位

本系统是一个面向课堂学习行为监测的桌面应用。它使用 Tkinter 构建界面，使用 YOLOv8 模型识别图片、摄像头、视频和屏幕画面中的学习行为，并通过 SQLite 数据库记录用户、日志、检测结果、误报样本、学习会话和行为明细。

系统当前重点支持以下能力：

- 用户登录、注册、密码修改和管理员账号管理
- 图片检测、批量图片/视频检测
- 摄像头实时检测和屏幕识别
- 检测记录、操作日志、数据库查询
- 误报样本保存和数据库登记
- 网络连通性、URL 检测、文件/图片下载测试
- 训练图片采集、图片预处理、YOLO 数据集校验和预览
- FP16 / INT8 模型量化与性能对比
- 告警统计图、Word 报告和 PDF 报告导出

## 2. 模块关系

系统按职责拆分为多个 Python 文件：

| 文件 | 职责 |
|---|---|
| `app.py` | 程序启动入口，运行 Tkinter 应用 |
| `ui.py` | 主界面、页面切换、按钮事件、后台任务调度 |
| `ai_core.py` | YOLO 模型加载、图片/视频/帧检测、行为后处理规则 |
| `db.py` | SQLite 建表和所有数据库读写方法 |
| `auth.py` | 用户注册、登录、密码和角色权限管理 |
| `network_utils.py` | URL 可达性、批量检测、下载和连通性测试 |
| `crawler.py` | 根据关键词采集训练图片 |
| `preprocess.py` | 图片校验、去重、缩放和训练/验证集划分 |
| `dataset_validator.py` | YOLO 数据集结构校验和预览图生成 |
| `network_tools.py` | 命令行网络诊断工具 |
| `visualization.py` | 告警统计图、模型量化图、性能对比图导出 |

主调用关系如下：

```text
app.py
  -> ui.py
      -> auth.py
      -> db.py
      -> ai_core.py
      -> network_utils.py
      -> crawler.py / preprocess.py / dataset_validator.py
      -> visualization.py
```

## 3. UI 实现

UI 使用 Tkinter 和 ttk 实现，主类是 `MainFrame`。

界面结构分为两部分：

- 左侧工具栏：负责进入检测、记录、网络监测、数据库查询、数据工具、结果导出、账号管理等功能组。
- 右侧主台：用于显示首页仪表盘、功能页面、表格、说明文本、模型对比和实时摄像头画面。

为了避免旧式弹窗过多，系统把大量功能从小弹窗改成右侧主台完整页面。旧方法 `_create_popup()` 仍然保留方法名，但实际作用已经变成“在右侧主台打开页面”，这样兼容旧代码，也提升了界面整体一致性。

UI 还支持字体视觉缩放：

- `adjust_workspace_zoom()` 调整视觉比例
- `apply_visual_zoom()` 递归刷新控件字体
- 缩放只改变显示效果，不改变检测数据和数据库内容

## 4. AI 检测核心实现

AI 检测由 `ai_core.py` 中的 `StudyBehaviorDetector` 完成。

核心流程：

1. `load_model()` 懒加载 `models/best.pt`
2. `predict_frame()` 对单帧图片运行 YOLO 推理
3. 根据类别和置信度生成 `DetectionEvent`
4. 对不同类别应用业务规则
5. 返回带框图像、事件列表和 summary 汇总信息

主要规则：

- `phone`：达到手机置信度阈值后直接作为高优先级告警
- `sleep`：除了置信度，还检查检测框横纵比例和持续帧数
- `eat`：结合检测置信度、ROI 运动量和滑动窗口比例判断

输出数据包括：

```text
events: 每个检测框对应的行为事件
summary: 总事件数、告警类别、吃东西比例、睡觉持续帧数、时间戳
annotated image/frame: 画好检测框的图像
```

## 5. 图片检测和批量检测

图片检测入口在 `ui.py`：

- `detect_image()`
- `_detect_image_worker()`
- `_handle_detection_done()`

实现方式：

1. 用户选择图片
2. UI 调用 `_run_task()` 开后台线程
3. 后台线程中创建 `StudyBehaviorDetector`
4. 调用 `predict_image()`
5. 保存检测输出图
6. 写入 `detection_records`
7. 写入 `study_behavior_records`
8. 自动检查是否需要保存疑似误报样本
9. 主线程弹出检测完成提示并刷新首页

批量检测使用 `ThreadPoolExecutor`：

- 图片并发处理
- 视频顺序处理
- 失败文件不会中断已完成任务

## 6. 摄像头检测和屏幕识别

摄像头检测和屏幕识别都属于实时检测任务。

核心状态：

```text
camera_session
screen_session
camera_queue
camera_stop_event
screen_stop_event
```

运行流程：

1. 用户点击启动
2. 创建学习会话 `start_study_session()`
3. 创建后台线程读取摄像头或屏幕画面
4. 每帧调用 `predict_frame()`
5. 通过 `Queue` 把最新帧传回主线程
6. 主线程定时 `_poll_camera_frames()` 刷新画面和统计信息
7. 用户停止后写入汇总检测记录
8. 写入行为明细
9. 结束学习会话 `end_study_session()`
10. 清空画面，避免保留最后一帧造成误解

屏幕识别支持拖拽框选区域：

- `_select_screen_region_by_drag()` 获取用户选择区域
- 识别时自动最小化主窗口
- 保留一个小控制窗显示当前结果、帧率和停止按钮

## 7. 多任务并发机制

Tkinter 主线程不能执行耗时任务，否则窗口会卡死。因此系统统一使用：

```python
threading.Thread
ThreadPoolExecutor
Queue
after()
```

关键方法：

- `_run_task()`：所有耗时任务的统一入口
- `_begin_task()`：增加后台任务计数，更新状态
- `_finish_task()`：减少后台任务计数，恢复 Ready 状态
- `_poll_camera_frames()`：主线程定时取实时画面

设计原则：

- 后台线程负责检测、下载、导出、爬虫等耗时操作
- Tk 主线程只负责界面更新
- 后台线程不能直接更新 Tk 控件，需要通过 `self.after(0, callback)` 回到主线程
- 实时画面通过小队列传递，队列满时丢弃旧帧，保证界面实时

## 8. 数据库功能

数据库由 `db.py` 管理，文件默认位于：

```text
project_data/study_monitor.db
```

主要表：

| 表 | 作用 |
|---|---|
| `users` | 用户账号、密码哈希、角色 |
| `operation_logs` | 用户登录、检测、导出、网络请求等操作日志 |
| `detection_records` | 每次检测任务的汇总结果 |
| `model_resources` | 模型或远程资源 URL 状态 |
| `misclassified_samples` | 误识别/误报样本 |
| `training_cycles` | 模型再训练周期 |
| `data_reflux_log` | 数据回流记录 |
| `study_sessions` | 一次持续学习监测会话 |
| `study_behavior_records` | 每条行为识别明细 |

当前 UI 已经接入：

- 数据库概览
- 误报样本查看
- 学习会话查看
- 行为明细查看
- 训练周期查看

## 9. 日志检测与异常监控

系统中的“日志检测”主要由 `operation_logs` 实现。

每次关键行为都会调用：

```python
self.db.log_operation(user_id, action, detail)
```

记录内容包括：

- 用户登录和注册
- 图片检测
- 批量检测
- 摄像头开始/停止
- 屏幕识别开始/停止
- 网络检测
- 文件下载
- 图片下载
- 图表导出
- 报告导出
- 模型量化
- 模型性能测试
- 误报样本保存
- 用户管理操作

日志页面通过 `show_logs()` 展示最近操作，并支持导出 CSV。

异常处理方式：

- 文件、网络、模型推理失败会进入 `_show_error()`
- 后台任务失败会写入 `operation_logs`
- 失败不会直接导致整个 GUI 退出

## 10. 误报样本和数据回流

误报样本来源：

- 图片检测中低置信度框
- 单张图检测框数量异常偏多
- 用户在摄像头/屏幕识别时点击保存误报帧
- 用户手动保存误识别样本

保存内容：

```text
原图或实时帧
预测图
metadata.json
预测事件
summary
用户信息
原因说明
数据库记录
```

数据库中写入 `misclassified_samples`，后续可人工审核、修正标注，并通过 `data_reflux_log` 记录回流到训练集的路径。

## 11. 网络功能

网络功能来自 `network_utils.py`，当前 UI 已接入：

- 网络连通性测试
- 单 URL 可达性检测
- 批量 URL 检测
- 文件下载测试
- 图片下载测试

这些功能主要用于：

- 验证模型或数据集下载链接
- 测试网络是否可用
- 下载图片作为训练或测试素材
- 记录远程资源状态

## 12. 数据工具

数据工具用于训练数据准备，不直接影响当前模型推理。

当前入口：

- 采集训练图片：调用 `crawler.py`
- 预处理图片集：调用 `preprocess.py`
- 校验数据集：调用 `dataset_validator.py`
- 生成数据预览：输出带标注框的预览图

完整数据准备链路：

```text
网络采集图片
  -> 图片校验
  -> 去重
  -> 缩放
  -> 划分 train/val
  -> YOLO 标签校验
  -> 数据集预览
  -> 后续模型训练
```

## 13. 模型量化和性能对比

模型对比页面支持：

- FP32 原始模型
- FP16 半精度模型
- INT8 量化权重包

可以统计：

- 模型文件大小
- 张量存储大小
- 加载耗时
- 平均推理耗时
- 最快推理耗时
- 最慢推理耗时
- 检测框数量
- 检测类别
- 平均置信度

结果可导出为图表，便于说明模型轻量化前后的差异。

## 14. 图表和报告导出

图表由 `visualization.py` 生成，支持：

- 柱状图
- 饼状图
- 折线图
- 模型量化对比图
- 模型性能对比图

报告导出支持：

- Word
- PDF
- 同时导出 Word 和 PDF

报告内容包括：

- 检测记录数
- 告警总数
- 最高频异常类别
- 自动分析结论
- 异常类别统计
- 最近检测记录
- 模型量化摘要
- 模型性能摘要
- 最近系统日志

告警统计优先使用 `study_behavior_records`，如果没有行为明细，则退回使用 `detection_records`。

## 15. 当前本地调试建议

建议按以下顺序测试：

1. 登录 admin/admin123
2. 注册普通用户
3. 图片检测
4. 查看检测记录
5. 查看行为明细
6. 保存误报帧或误识别样本
7. 查看误报样本
8. 摄像头检测
9. 屏幕识别
10. 网络连通性测试
11. URL 可达性检测
12. 图表导出
13. Word/PDF 报告导出
14. 模型 FP16/INT8 对比

## 16. 功能之间的闭环关系

系统目前形成了以下闭环：

```text
检测输入
  -> YOLO 推理
  -> 检测事件 events
  -> 检测汇总 detection_records
  -> 行为明细 study_behavior_records
  -> 图表和报告统计
```

```text
检测误报
  -> 保存误报样本
  -> misclassified_samples
  -> 人工复核
  -> data_reflux_log
  -> 后续训练周期 training_cycles
```

```text
实时监测
  -> start_study_session
  -> 逐帧识别
  -> 行为采样
  -> end_study_session
  -> 学习会话统计
```

```text
网络/数据工具
  -> 图片采集
  -> 预处理
  -> 数据集校验
  -> 数据预览
  -> 模型训练准备
```

整体上，系统已经从“单次检测工具”扩展为“检测、记录、分析、误报回流、模型优化准备”的完整应用。
