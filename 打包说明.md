# 程序打包与分发说明

本文档说明如何把本项目打包为 Windows/macOS 可执行程序。打包后的程序可以在没有 Python 环境的电脑上运行；构建打包文件的电脑仍然需要安装 Python 和项目依赖。

## 一、推荐打包方式

推荐使用 PyInstaller 的目录式打包方式，也就是生成 `dist/StudyBehaviorMonitor/` 整个文件夹。

不建议默认使用单文件 `-F`，因为本项目包含 `torch`、`ultralytics`、`opencv-python`、`matplotlib` 等大型依赖。单文件包通常体积更大、启动更慢，也更容易被杀毒软件误报。

## 二、打包前检查

打包前请确认这些文件存在：

```text
app.py
ui.py
ai_core.py
db.py
auth.py
network_utils.py
visualization.py
data.yaml
models/best.pt
requirements.txt
requirements-build.txt
packaging/StudyBehaviorMonitor.spec
```

其中 `models/best.pt` 是默认 YOLO 权重文件。`project_data/`、`outputs/`、`reports/`、`result/`、`relabel/` 属于运行时生成目录，不建议直接打进安装包。

## 三、Windows 打包

在 PowerShell 中进入项目目录：

```powershell
cd C:\Users\HD\Desktop\python_experiment\study-behavior-monitor-plus
```

安装依赖并执行构建：

```powershell
powershell -ExecutionPolicy Bypass -File .\packaging\build_windows.ps1
```

如果你使用的是本机已有的 `dev` 环境，建议显式指定 Python 路径，避免误用其他 Python：

```powershell
powershell -ExecutionPolicy Bypass -File .\packaging\build_windows.ps1 -Python C:\Users\HD\miniforge3\envs\dev\python.exe
```

如果依赖已经安装好，可以跳过安装步骤：

```powershell
powershell -ExecutionPolicy Bypass -File .\packaging\build_windows.ps1 -Python C:\Users\HD\miniforge3\envs\dev\python.exe -SkipInstall
```

构建成功后，可执行文件位置为：

```text
dist/StudyBehaviorMonitor/StudyBehaviorMonitor.exe
```

分发时不要只复制 `StudyBehaviorMonitor.exe`，而是复制整个 `dist/StudyBehaviorMonitor/` 文件夹。这个文件夹里包含程序运行所需的动态库、Python 运行时、模型文件和资源文件。

## 四、macOS 打包

macOS 应用必须在 macOS 机器上构建，不能在 Windows 上直接生成可用的 `.app`。

在终端中进入项目目录：

```bash
cd /path/to/study-behavior-monitor-plus
chmod +x packaging/build_macos.sh
./packaging/build_macos.sh
```

如果要指定某个 Python 解释器，可以这样运行：

```bash
PYTHON_BIN=/path/to/python3 ./packaging/build_macos.sh
```

构建成功后，应用位置通常为：

```text
dist/StudyBehaviorMonitor/StudyBehaviorMonitor.app
```

macOS 首次运行屏幕识别或摄像头检测时，可能需要在系统设置中授予屏幕录制、摄像头和文件访问权限。

## 五、cx_Freeze 备用方案

如果 PyInstaller 在某台电脑上无法正确打包，可以尝试 cx_Freeze：

```powershell
python .\packaging\cx_freeze_setup.py build
```

cx_Freeze 会生成 `build/` 目录。分发时同样需要复制整个生成目录，而不是只复制单个可执行文件。

## 六、模型和数据库说明

默认打包只包含：

```text
models/best.pt
data.yaml
```

`models/best_fp16.pt`、`models/best_int8_quantized.pt`、`models/benchmark_report.json`、`models/quantization_report.json` 属于运行后生成或实验结果文件，不作为默认打包资源。如果需要演示量化对比功能，可以先在开发机生成这些文件，再按需要手动加入发布包。

程序首次启动时会自动创建：

```text
project_data/study_monitor.db
```

因此分发包不需要预置数据库。打包后，`project_data/`、`outputs/`、`reports/`、`relabel/` 会生成在 `StudyBehaviorMonitor.exe` 所在目录，便于用户查看和备份。若需要携带已有账号、检测记录或日志，可以把已有 `project_data/` 目录复制到发布目录旁边，但要注意隐私数据。

## 七、常见问题

### 1. 打包时报 SSL 或 pip 下载失败

可以临时切换 PyPI 官方源：

```powershell
python -m pip install -r requirements.txt -i https://pypi.org/simple
python -m pip install -r requirements-build.txt -i https://pypi.org/simple
```

如果学校或公司网络拦截 HTTPS，需要更换网络或配置可信证书。

### 2. exe 启动后提示找不到模型

请确认发布目录中存在：

```text
dist/StudyBehaviorMonitor/models/best.pt
```

如果模型文件不在包内，可以手动创建 `models/` 目录并放入 `best.pt`。

### 3. 摄像头打不开

请确认摄像头没有被其他软件占用，并检查系统隐私权限。Windows 可在“设置 - 隐私和安全性 - 摄像头”中打开应用权限。

### 4. 屏幕识别不可用

请确认已经安装 `mss`，并在 macOS 中授予屏幕录制权限。Windows 下如果截图区域异常，优先检查缩放比例和多显示器设置。

### 5. 程序包体积很大

这是正常现象。`torch`、`ultralytics` 和 OpenCV 本身较大。如果构建环境安装的是 CUDA 版 PyTorch，发布目录可能达到数 GB。课程展示或答辩时建议使用目录式发布包，稳定性优先于体积。

如果只需要 CPU 推理，可以新建一个 CPU-only 的 PyTorch 环境重新打包，通常能明显降低体积。

## 八、交付清单

推荐最终交付内容：

```text
dist/StudyBehaviorMonitor/
打包说明.md
```

如果交付源码，则同时提交：

```text
requirements.txt
requirements-build.txt
packaging/StudyBehaviorMonitor.spec
packaging/build_windows.ps1
packaging/build_macos.sh
packaging/cx_freeze_setup.py
```

不要提交 `build/`、`dist/`、`project_data/`、`outputs/`、`reports/`、`result/`、`relabel/` 等生成目录。
